akka.actor.provider = "akka.actor.LocalActorRefProvider"

analyzer {
  buffer {
    interval = 1 second
    initial-delay = 100 milliseconds
  }

  activity-check {
    interval = 1 second
    initial-delay = 100 milliseconds
  }
}